# ── Data & ML ─────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.0
duckdb>=0.10
pyarrow>=15.0
statsmodels>=0.14

# ── RAG & Embeddings ──────────────────────────────────────────────────────────
langchain>=0.2
langchain-google-genai>=1.0       # Gemini LLM + embeddings via LangChain
langchain-community>=0.2
llama-index>=0.10
llama-index-vector-stores-qdrant>=0.2
sentence-transformers>=3.0        # Local embeddings (no API cost)
qdrant-client>=1.9                # Vector database client

# ── GenAI evaluation ──────────────────────────────────────────────────────────
ragas>=0.1                        # RAG pipeline evaluation metrics

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7

# ── MLflow experiment tracking ────────────────────────────────────────────────
mlflow>=2.13

# ── Notebooks & visualisation ─────────────────────────────────────────────────
jupyter>=1.0
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20

# ── Dev tooling ───────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
python-dotenv>=1.0
kaggle>=1.6                       # Kaggle CLI for dataset downloads
